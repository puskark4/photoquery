
// This component is no longer used in the simplified single-photo flow.
// It can be safely deleted. If Next.js complains, remove imports.

// Minimal content to prevent build errors if still imported somewhere temporarily.
export function PhotoCard() {
  return null;
}
